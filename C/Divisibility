#include<stdio.h>
int main()
{
    int num[100000],i,r,j,res=0,n;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&num[i]);
    }
    res=num[n-1]%10;
    if(res==0)
    printf("Yes");
    else
    printf("No");
    return 0;
}
